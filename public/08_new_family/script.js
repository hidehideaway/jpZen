(function() {
    const API_BASE_URL = 'http://localhost:8040';

    window.initFamilyTree = function(members) {
        // この関数は空になります。React側で処理を行うため。
        console.log('Family tree initialization requested with members:', members);
    };
})();